import React, { Component, Text } from 'react-native';

export default class ProfilePage extends Component {
  render() {
    return(
      <Text style={{backgroundColor: '#000', color: 'white', margin: 50}}>
        Profile Page
      </Text>
    )
  }
}
